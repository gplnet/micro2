spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/datantt
    username: postgres
    password: Realmadrid2022*.
    
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
      

server:
   port: 8799


   

    
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka